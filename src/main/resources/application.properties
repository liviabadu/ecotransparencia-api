# =============================================================================
# EcoTransparencia API - Application Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# HTTP Server Configuration (Cloud Run compatibility)
# -----------------------------------------------------------------------------
# Cloud Run injects PORT env var - use it, default to 8080
quarkus.http.port=${PORT:8080}
quarkus.http.host=0.0.0.0

# -----------------------------------------------------------------------------
# Default Datasource (PostgreSQL).
# Sem URL/user/password default: em %dev quem provem e' o DevServices (PostGIS),
# em %prod sao env vars obrigatorias (DB_URL/DB_USER/DB_PASSWORD), em %test e' H2.
# Definir um URL aqui suprimiria o DevServices em quarkus:dev.
# -----------------------------------------------------------------------------
quarkus.datasource.db-kind=postgresql

# Schema managed exclusively by Flyway (no Hibernate auto-DDL)
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.log.sql=false

# Flyway
quarkus.flyway.migrate-at-start=true
quarkus.flyway.baseline-on-migrate=true

# DevServices: PostGIS image para `quarkus:dev`
quarkus.datasource.devservices.image-name=postgis/postgis:16-3.4
quarkus.datasource.devservices.db-name=ecotransparencia

# -----------------------------------------------------------------------------
# Production Profile (Cloud SQL via env vars; fail-fast on missing config)
# -----------------------------------------------------------------------------
%prod.quarkus.datasource.jdbc.url=${DB_URL}
%prod.quarkus.datasource.username=${DB_USER}
%prod.quarkus.datasource.password=${DB_PASSWORD}

# -----------------------------------------------------------------------------
# Dev Profile
# -----------------------------------------------------------------------------
%dev.quarkus.hibernate-orm.log.sql=true

# -----------------------------------------------------------------------------
# Test Profile (keeps H2 + Flyway disabled; suite untouched)
# -----------------------------------------------------------------------------
%test.quarkus.datasource.db-kind=h2
%test.quarkus.datasource.username=sa
%test.quarkus.datasource.password=
%test.quarkus.datasource.jdbc.url=jdbc:h2:mem:ecotransparencia-test;DB_CLOSE_DELAY=-1;MODE=MySQL
%test.quarkus.flyway.enabled=false
%test.quarkus.hibernate-orm.database.generation=drop-and-create
%test.quarkus.hibernate-orm.log.sql=false
%test.app.data.load-on-startup=false
%test.app.data.load-autos-on-startup=false

# -----------------------------------------------------------------------------
# IBAMA Data Loading - Embargos (US-INFRA-001)
# -----------------------------------------------------------------------------
# Enable automatic data loading on startup
app.data.load-on-startup=false

# Path to IBAMA CSV file (areas embargadas)
app.data.csv-path=docs/ibama/areas_embargadas.csv

# Dev profile: enable data loading
%dev.app.data.load-on-startup=true

# Prod profile: enable data loading from container path
%prod.app.data.load-on-startup=true
%prod.app.data.csv-path=/deployments/data/areas_embargadas.csv

# -----------------------------------------------------------------------------
# IBAMA Data Loading - Autos de Infracao
# -----------------------------------------------------------------------------
# Enable automatic loading of auto infracao data on startup
app.data.load-autos-on-startup=true

# Directory containing auto_infracao_ano_YYYY.csv files
app.data.autos-csv-dir=docs/ibama/autos

# Dev profile: enable auto infracao loading
%dev.app.data.load-autos-on-startup=true

# Prod profile: enable auto infracao loading from container path
%prod.app.data.load-autos-on-startup=true
%prod.app.data.autos-csv-dir=/deployments/data/autos

# -----------------------------------------------------------------------------
# Fase B: CEIS/CNEP/CEPIM (Portal da Transparencia)
# -----------------------------------------------------------------------------
# Diretorio com os CSVs do Portal da Transparencia (prefixo de data muda
# a cada refresh; loaders fazem glob por padrao). Default em dev local;
# em %prod aponta para o diretorio de dados do container.
app.data.adm-publica-csv-dir=docs/adm_publica
%prod.app.data.adm-publica-csv-dir=/deployments/data/adm_publica

# CEIS + CNEP loader (entity unica SancaoAdmPublica)
app.data.load-sancao-adm-publica-on-startup=false
%dev.app.data.load-sancao-adm-publica-on-startup=true
%prod.app.data.load-sancao-adm-publica-on-startup=true

# CEPIM loader
app.data.load-cepim-on-startup=false
%dev.app.data.load-cepim-on-startup=true
%prod.app.data.load-cepim-on-startup=true

# -----------------------------------------------------------------------------
# Fase B: MTE (Lista Suja Trabalho Escravo)
# -----------------------------------------------------------------------------
app.data.mte-csv-path=docs/mte/tr_escravo.csv
%prod.app.data.mte-csv-path=/deployments/data/mte/tr_escravo.csv

app.data.load-trabalho-escravo-on-startup=false
%dev.app.data.load-trabalho-escravo-on-startup=true
%prod.app.data.load-trabalho-escravo-on-startup=true

# Test profile: tudo desabilitado (carga manual via fixtures nos testes)
%test.app.data.load-sancao-adm-publica-on-startup=false
%test.app.data.load-cepim-on-startup=false
%test.app.data.load-trabalho-escravo-on-startup=false
%test.app.data.load-icmbio-on-startup=false

# -----------------------------------------------------------------------------
# Fase C: ICMBio (autos + embargos via XLSX para atributos + SHP para geometria)
# -----------------------------------------------------------------------------
app.data.load-icmbio-on-startup=false
app.data.icmbio-autos-xlsx-path=docs/icmbio/autos_infracao_icmbio.xlsx
app.data.icmbio-autos-shp-path=docs/icmbio/autos_infracao_icmbio_shp/autos_infracao_icmbio.shp
app.data.icmbio-embargos-xlsx-path=docs/icmbio/embargos_icmbio.xlsx
app.data.icmbio-embargos-shp-path=docs/icmbio/embargos_icmbio_shp/embargos_icmbio_shp.shp

%dev.app.data.load-icmbio-on-startup=true

%prod.app.data.load-icmbio-on-startup=true
%prod.app.data.icmbio-autos-xlsx-path=/deployments/data/icmbio/autos_infracao_icmbio.xlsx
%prod.app.data.icmbio-autos-shp-path=/deployments/data/icmbio/autos_infracao_icmbio_shp/autos_infracao_icmbio.shp
%prod.app.data.icmbio-embargos-xlsx-path=/deployments/data/icmbio/embargos_icmbio.xlsx
%prod.app.data.icmbio-embargos-shp-path=/deployments/data/icmbio/embargos_icmbio_shp/embargos_icmbio_shp.shp

# -----------------------------------------------------------------------------
# Startup Timeout Configuration
# -----------------------------------------------------------------------------
# Increase startup timeout to allow data loading (default is 60s)
quarkus.startup-timeout=300s

# Transaction timeout for large batch operations (default is 60s)
quarkus.transaction-manager.default-transaction-timeout=600s

# Dev profile: longer timeout for data loading
%dev.quarkus.startup-timeout=300s
%dev.quarkus.transaction-manager.default-transaction-timeout=600s

# Prod profile: longer timeout for data loading
%prod.quarkus.startup-timeout=300s
%prod.quarkus.transaction-manager.default-transaction-timeout=600s

# -----------------------------------------------------------------------------
# OpenAPI / Swagger UI (US-INFRA-002)
# -----------------------------------------------------------------------------
quarkus.smallrye-openapi.info-title=EcoTransparencia API
quarkus.smallrye-openapi.info-version=1.0.0
quarkus.smallrye-openapi.info-description=API para consulta de entidades com areas embargadas pelo IBAMA. \
  Permite buscar pessoas fisicas e juridicas que possuem registros de embargo ambiental.
quarkus.smallrye-openapi.info-contact-name=EcoTransparencia Team
quarkus.smallrye-openapi.info-license-name=Apache 2.0
quarkus.smallrye-openapi.info-license-url=https://www.apache.org/licenses/LICENSE-2.0

# Swagger UI: habilitado em todos os ambientes
quarkus.swagger-ui.always-include=true

# -----------------------------------------------------------------------------
# Receita Federal API - CNPJA API Open (US-007)
# Documentacao: https://cnpja.com/api/open
# -----------------------------------------------------------------------------
# Habilita uso da API real (false = usa stub que sempre retorna ATIVA)
ecotransparencia.receita-federal.use-real-api=false

# Configuracao do REST Client para CNPJA API Open
quarkus.rest-client.cnpja-api.url=https://open.cnpja.com
quarkus.rest-client.cnpja-api.scope=jakarta.inject.Singleton

# Timeout para consultas externas
quarkus.rest-client.cnpja-api.connect-timeout=5000
quarkus.rest-client.cnpja-api.read-timeout=10000

# Prod profile: habilita API real
%prod.ecotransparencia.receita-federal.use-real-api=true
